* Asterisk requests should be POSTs, urlencode! (Not really needed)
* Add asterisk provision to fabric
	* sip.conf (Be careful)
	* extensions.conf
	* manager.conf
	* http.conf
* Caching! Flask cache...
* Access Redis via unix socket, if Redis is local 
* Import settings in fabric and use it
* Sanitize message to avoid Asterisk injections.
* Create a Event DB representation, add action_config_id, to know the last action executed. Add status. Invent a way to identify the event sent by the client, not taking into account the step. Anoter hash?
* Move actions functionality to models... Nope (circular imports!), but copy object
* Add POST to API